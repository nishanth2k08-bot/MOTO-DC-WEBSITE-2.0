(()=>{const reduce=window.matchMedia?.('(prefers-reduced-motion: reduce)').matches;if(reduce)return;const selectors=['.sectionhead','.section>h2','.section>p','.categories button','.grid .card','.filters','.checkoutBlock','.checkoutSummary','.cartitem','.cartlayout aside','.detail','.orderCard','.ordersEmpty','.adminproduct','.dashPanel','.customerRow:not(.customerHeader)','.aboutHero','.aboutGrid>.aboutCard','.authcard','.accountcard'];const apply=()=>{const nodes=[];for(const selector of selectors){document.querySelectorAll(selector).forEach(el=>{if(!el.classList.contains('moto-visible')&&!el.classList.contains('moto-reveal'))nodes.push(el)})}nodes.forEach((el,i)=>{el.classList.add('moto-reveal');const n=i%5;if(n)el.classList.add(`moto-stagger-${n}`)});if(!window.__motoRevealObserver){window.__motoRevealObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;entry.target.classList.add('moto-visible');window.__motoRevealObserver.unobserve(entry.target)})},{root:null,rootMargin:'0px 0px -8% 0px',threshold:.08})}document.querySelectorAll('.moto-reveal:not(.moto-visible)').forEach(el=>window.__motoRevealObserver.observe(el))};let scheduled=false;const schedule=()=>{if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;apply()})};apply();new MutationObserver(schedule).observe(document.getElementById('root')||document.body,{childList:true,subtree:true});window.addEventListener('pageshow',schedule);window.addEventListener('popstate',schedule)})();

/* Keep the theme class synchronized on both html and body. */
(()=>{const syncTheme=()=>{const light=localStorage.getItem('motodc-theme')==='light';document.documentElement.classList.toggle('light-theme',light);if(document.body)document.body.classList.toggle('light-theme',light)};syncTheme();if(document.body)new MutationObserver(syncTheme).observe(document.body,{attributes:true,attributeFilter:['class']});window.addEventListener('storage',syncTheme)})();