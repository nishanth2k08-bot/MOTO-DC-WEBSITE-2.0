import admin from 'firebase-admin';
function getAdmin(){if(!admin.apps.length){const raw=process.env.FIREBASE_SERVICE_ACCOUNT_JSON;if(!raw)throw new Error('Missing FIREBASE_SERVICE_ACCOUNT_JSON');admin.initializeApp({credential:admin.credential.cert(JSON.parse(raw)))});}return admin;}
function send(res,status,body){res.status(status).json(body)}
export default async function handler(req,res){if(req.method!=='POST')return send(res,405,{error:'Method not allowed'});try{const {items,amount}=req.body||{};const auth=req.headers.authorization||'';const idToken=auth.startsWith('Bearer ')?auth.slice(7):'';if(!idToken)return send(res,401,{error:'Authentication required'});if(!Array.isArray(items)||!items.length)return send(res,400,{error:'Cart is empty'});if(!Number.isFinite(Number(amount))||Number(amount)<=0)return send(res,400,{error:'Invalid amount'});const a=getAdmin(),decoded=await a.auth().verifyIdToken(idToken),db=a.firestore();const refs=items.map(x=>db.collection('products').doc(String(x.id)));const snaps=await db.getAll(...refs);let total=0;const safeItems=[];for(let i=0;i<snaps.length;i++){const snap=snaps[i],requested=Math.max(1,Math.floor(Number(items[i].qty||1)));if(!snap.exists)return send(res,400,{error:'A product is no longer available'});const p=snap.data(),stock=Number(p.stock||0);if(requested>stock)return send(res,400,{error:`Insufficient stock for ${p.name}`});const price=Number(p.price||0);total+=price*requested;safeItems.push({id:snap.id,name:p.name,category:p.category||'',price,qty:requested,image:p.image||''});}if(total!==Number(amount))return send(res,400,{error:'Cart total changed. Please refresh and try again.'});const keyId=process.env.RAZORPAY_KEY_ID,keySecret=process.env.RAZORPAY_KEY_SECRET;if(!keyId||!keySecret)return send(res,503,{error:'Online payment is not configured yet'});const receipt=`MDC-${Date.now()}-${decoded.uid.slice(0,8)}`;const response=await fetch('https://api.razorpay.com/v1/orders',{method:'POST',headers:{Authorization:`Basic ${Buffer.from(`${keyId}:${keySecret}`).toString('base64')}`,'Content-Type':'application/json'},body:JSON.stringify({amount:Math.round(total*100),currency:'INR',receipt,notes:{userId:decoded.uid}})});const data=await response.json();if(!response.ok)return send(res,502,{error:data.error?.description||'Payment gateway error'});await db.collection('paymentIntents').doc(data.id).set({userId:decoded.uid,amount:total,currency:'INR',items:safeItems,status:'created',createdAt:admin.firestore.FieldValue.serverTimestamp()});return send(res,200,{keyId,orderId:data.id,amount:data.amount,currency:data.currency});}catch(e){console.error(e);return send(res,500,{error:'Could not create payment order'});}}
