import React,{useEffect,useState} from 'react';import {collection,getDocs,query,where,orderBy,deleteDoc,doc} from 'firebase/firestore';import {adminDb} from './firebase';import {Package,ChevronDown,Trash2,MapPin,Phone,Mail} from 'lucide-react';import {toast} from 'react-hot-toast';import './admin-dashboard.css';const money=n=>'₹'+Number(n||0).toLocaleString('en-IN');const names=['codOrders','onlineOrders'];
export default function AdminCompletedOrders(){const [orders,setOrders]=useState([]),[loading,setLoading]=useState(true),[open,setOpen]=useState(null),[busy,setBusy]=useState('');const load=async()=>{setLoading(true);try{const snaps=await Promise.all(names.map(n=>getDocs(query(collection(adminDb,n),where('orderStatus','==','delivered'),orderBy('createdAt','desc'))));setOrders(snaps.flatMap((s,i)=>s.docs.map(d=>({id:d.id,...d.data(),paymentMethod:i?'online':'cod'}))).sort((a,b)=>(b.createdAt?.toMillis?.()||0)-(a.createdAt?.toMillis?.()||0)))}catch(e){console.error(e);toast.error('Could not load completed orders')}finally{setLoading(false)}};useEffect(()=>{load()},[]);const remove=async o=>{if(!window.confirm(`Delete completed order ${o.orderId||o.id} permanently from Firebase?`))return;setBusy(o.id);try{await deleteDoc(doc(adminDb,o.paymentMethod==='online'?'onlineOrders':'codOrders',o.id));setOrders(xs=>xs.filter(x=>!(x.id===o.id&&x.paymentMethod===o.paymentMethod)));toast.success('Completed order deleted')}catch(e){console.error(e);toast.error('Could not delete completed order. Check Firebase rules.')}finally{setBusy('')}};return <div className="completedOrdersSection"><div className="ordersHead"><div><p className="eyebrow">ARCHIVE</p><h2>Completed <span>orders.</span></h2><p>Delivered orders are kept here separately from active order management.</p></div><button className="adminsignout" onClick={load} disabled={loading}><Package size={16}/> {loading?'Loading...':'Refresh'}</button></div>{loading?<div className="empty">Loading completed orders...</div>:!orders.length?<div className="ordersEmpty"><Package size={40}/><h3>No completed orders</h3><p>Delivered orders will appear here.</p></div>:<div className="ordersList">{orders.map(o=><div className={`orderCard ${open?.id===o.id&&open?.paymentMethod===o.paymentMethod?'expanded':''}`} key={`${o.paymentMethod}-${o.id}`}><div className="orderTop"><div><b>{o.orderId||o.id}</b><small>{o.customer?.name||'Customer'} · {o.customer?.phone||'No phone'}</small></div><strong>{money(o.total)}</strong><span className="status delivered">Delivered</span><button className="orderToggle" onClick={()=>setOpen(open?.id===o.id&&open?.paymentMethod===o.paymentMethod?null:{id:o.id,paymentMethod:o.paymentMethod})}><ChevronDown size={18}/></button><button className="adminOrderDelete" onClick={()=>remove(o)} disabled={busy===o.id} title="Delete completed order"><Trash2 size={17}/></button></div>{open?.id===o.id&&open?.paymentMethod===o.paymentMethod&&<div className="orderDetails"><div className="detailGrid"><div><h4>Customer</h4><p><Mail size={14}/> {o.customer?.email||'—'}</p><p><Phone size={14}/> {o.customer?.phone||'—'}</p></div><div><h4>Delivery address</h4><p><MapPin size={14}/> {o.shipping?.address||'—'}, {o.shipping?.city||''}, {o.shipping?.state||''} - {o.shipping?.pincode||''}</p></div><div><h4>Payment</h4><p>{o.paymentMethod==='cod'?'Cash on Delivery':'Online Payment'} · {o.paymentStatus||'pending'}</p></div></div><div className="orderedItems"><h4>Items</h4>{(o.items||[]).map((x,i)=><div key={i}><img src={x.image} alt=""/><span><b>{x.name}</b><small>Qty {x.qty} · {money(x.price)} each</small></span><strong>{money(x.price*x.qty)}</strong></div>)}</div></div>}</div>)}</div>}</div>}
