import React,{useEffect,useState} from 'react';
import {collection,getDocs,query,where,orderBy,deleteDoc} from 'firebase/firestore';
import {adminDb} from './firebase';
import {Package,ChevronDown,Trash2,MapPin,Phone,Mail} from 'lucide-react';
import {toast} from 'react-hot-toast';
import './admin-dashboard.css';
const money=n=>'₹'+Number(n||0).toLocaleString('en-IN');
const records=p=>collection(adminDb,'orders',p==='online'?'online orders':'cod orders','records');
export default function AdminCompletedOrders(){const [orders,setOrders]=useState([]),[loading,setLoading]=useState(true),[open,setOpen]=useState(null),[busy,setBusy]=useState('');const load=async()=>{setLoading(true);try{const [cod,online]=await Promise.all([getDocs(query(records('cod'),where('orderStatus','==','delivered'),orderBy('createdAt','desc'))),getDocs(query(records('online'),where('orderStatus','==','delivered'),orderBy('createdAt','desc')))]);setOrders([...cod.docs,...online.docs].map(d=>({id:d.id,...d.data()})).sort((a,b)=>(b.createdAt?.toMillis?.()||0)-(a.createdAt?.toMillis?.()||0)))}catch(e){console.error(e);toast.error(e.code==='failed-precondition'?'Create the Firestore indexes for completed order records first':'Could not load completed orders')}finally{setLoading(false)}};useEffect(()=>{load()},[]);const remove=async o=>{if(!window.confirm(`Delete completed order ${o.orderId||o.id} permanently from Firebase?`))return;setBusy(o.id);try{await deleteDoc(doc(adminDb,'orders',o.paymentMethod==='online'?'online orders':'cod orders','records',o.id));setOrders(xs=>xs.filter(x=>x.id!==o.id));toast.success('Completed order deleted')}catch(e){console.error(e);toast.error('Could not delete completed order. Check Firebase rules.')}finally{setBusy('')}};return <div className="completedOrdersSection"><div className="ordersHead"><div><p className="eyebrow">ARCHIVE</p><h2>Completed <span>orders.</span></h2><p>Delivered orders are kept here separately from active order management.</p></div><button className="adminsignout" onClick={load} disabled={loading}><Package size={16}/> {loading?'Loading...':'Refresh'}</button></div>{loading?<div className="empty">Loading completed orders...</div>:!orders.length?<div className="ordersEmpty"><Package size={40}/><h3>No completed orders</h3><p>Delivered orders will appear here.</p></div>:<div className="ordersList">{orders.map(o=><div className={`orderCard ${open===o.id?'expanded':''}`} key={`${o.paymentMethod}-${o.id}`}><div className="orderTop"><div><b>{o.orderId||o.id}</b><small>{o.customer?.name||'Customer'} · {o.customer?.phone||'No phone'}</small></div><strong>{money(o.total)}</strong><span className="status delivered">Delivered</span><button className="orderToggle" onClick={()=>setOpen(open===o.id?null:o.id)}><ChevronDown size={18}/></button><button className="adminOrderDelete" onClick={()=>remove(o)} disabled={busy===o.id} title="Delete completed order"><Trash2 size={17}/></button></div>{open===o.id&&<div className="orderDetails"><div className="detailGrid"><div><h4>Customer</h4><p><Mail size={14}/> {o.customer?.email||'—'}</p><p><Phone size={14}/> {o.customer?.phone||'—'}</p></div><div><h4>Delivery address</h4><p><MapPin size={14}/> {o.shipping?.address||'—'}, {o.shipping?.city||''}, {o.shipping?.state||''} - {o.shipping?.pincode||''}</p></div><div><h4>Payment</h4><p>{o.paymentMethod==='cod'?'Cash on Delivery':'Online Payment'} · {o.paymentStatus||'pending'}</p></div></div><div className="orderedItems"><h4>Items</h4>{(o.items||[]).map((x,i)=><div key={i}><img src={x.image} alt=""/><span><b>{x.name}</b><small>Qty {x.qty} · {money(x.price)} each</small></span><strong>{money(x.price*x.qty)}</strong></div>)}</div></div>}</div>)}</div>}</div>}
