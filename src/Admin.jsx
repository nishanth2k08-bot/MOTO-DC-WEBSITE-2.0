import React,{useEffect,useState} from 'react';
import {collection,deleteDoc,doc,getDocs,addDoc,updateDoc,getDoc} from 'firebase/firestore';
import {onAuthStateChanged,signInWithEmailAndPassword,signOut} from 'firebase/auth';
import {adminAuth,adminDb} from './firebase';
import AdminOrders from './AdminOrders';
import {Plus,Trash2,LogOut,Edit3,X,Package,Database,Download} from 'lucide-react';
import {toast} from 'react-hot-toast';
import {majorSpareProducts} from './major-spares';
import './admin.css';

const empty={name:'',category:'Automobile',brand:'',fitment:'',price:'',rating:'5',stock:'0',image:'',description:''};
const num=v=>Number(v||0);
const demoProducts=[
 {name:'Racing Exhaust Muffler',category:'Motorcycle',price:6299,rating:4.6,stock:12,image:'/products/exhaust.svg',description:'Premium performance exhaust muffler for motorcycles'},
 {name:'LED Headlamp Assembly',category:'Automobile',price:3199,rating:4.7,stock:40,image:'/products/headlamp.svg',description:'High-brightness LED headlamp assembly for automobiles'},
 {name:'Alloy Wheel Rim 17"',category:'Automobile',price:8999,rating:4.9,stock:8,image:'/products/wheel.svg',description:'Premium 17-inch alloy wheel rim for improved style and performance'},
 {name:'Chain & Sprocket Kit',category:'Motorcycle',price:2799,rating:4.5,stock:33,image:'/products/chain.svg',description:'Durable chain and sprocket replacement kit for motorcycles'},
 {name:'Turbocharger Unit',category:'Automobile',price:24999,rating:4.9,stock:5,image:'/products/turbo.svg',description:'High-performance turbocharger unit for enhanced engine performance'},
 {name:'Sports Bike Fairing Kit',category:'Motorcycle',price:15999,rating:4.4,stock:6,image:'/products/fairing.svg',description:'Complete sports styling fairing kit for motorcycles'},
 {name:'High-Performance Air Filter',category:'Automobile',price:999,rating:4.3,stock:60,image:'/products/air-filter.svg',description:'High-flow air filter designed for improved engine airflow'}
];

export default function Admin(){
 const [user,setUser]=useState(null),[checking,setChecking]=useState(true),[allowed,setAllowed]=useState(false),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[loginBusy,setLoginBusy]=useState(false),[products,setProducts]=useState([]),[form,setForm]=useState(empty),[editing,setEditing]=useState(null),[saving,setSaving]=useState(false),[seeding,setSeeding]=useState(false),[bulkImporting,setBulkImporting]=useState(false);
 useEffect(()=>onAuthStateChanged(adminAuth,async u=>{setUser(u);setAllowed(false);if(!u){setChecking(false);return}try{const snap=await getDoc(doc(adminDb,'admins',u.uid));setAllowed(snap.exists())}catch(e){console.error(e);toast.error('Could not verify admin access')}finally{setChecking(false)}}),[]);
 useEffect(()=>{if(allowed)loadProducts()},[allowed]);
 async function loadProducts(){try{const snap=await getDocs(collection(adminDb,'products'));setProducts(snap.docs.map(d=>({id:d.id,...d.data()})))}catch(e){console.error(e);toast.error(e.code==='permission-denied'?'Admin permissions are not enabled yet':'Could not load products')}}
 async function login(e){e.preventDefault();setLoginBusy(true);try{await signInWithEmailAndPassword(adminAuth,email,password);toast.success('Admin signed in')}catch(e){toast.error(e.code==='auth/invalid-credential'?'Invalid email or password':e.message)}finally{setLoginBusy(false)}}
 function edit(p){setEditing(p.id);setForm({name:p.name||'',category:p.category||'Automobile',brand:p.brand||'',fitment:p.fitment||'',price:String(p.price??''),rating:String(p.rating??''),stock:String(p.stock??''),image:p.image||'',description:p.description||''});window.scrollTo({top:0,behavior:'smooth'})}
 function reset(){setEditing(null);setForm(empty)}
 async function save(e){e.preventDefault();if(!form.name.trim()||form.price===''||!form.image.trim()){toast.error('Name, price and image are required');return}setSaving(true);const payload={name:form.name.trim(),category:form.category,brand:form.brand.trim(),fitment:form.fitment.trim(),price:num(form.price),rating:num(form.rating),stock:num(form.stock),image:form.image.trim(),description:form.description.trim()};try{if(editing)await updateDoc(doc(adminDb,'products',editing),payload);else await addDoc(collection(adminDb,'products'),payload);toast.success(editing?'Product updated':'Product added');reset();await loadProducts()}catch(e){console.error(e);toast.error('Save failed. Check Firebase admin rules.')}finally{setSaving(false)}}
 async function seedDemoProducts(){if(seeding)return;setSeeding(true);try{const existing=await getDocs(collection(adminDb,'products'));const existingNames=new Set(existing.docs.map(d=>String(d.data().name||'').trim().toLowerCase()));const missing=demoProducts.filter(p=>!existingNames.has(p.name.toLowerCase()));if(!missing.length){toast.success('All demo products are already in the catalog');return}await Promise.all(missing.map(p=>addDoc(collection(adminDb,'products'),p)));toast.success(`${missing.length} demo product${missing.length===1?'':'s'} added`);await loadProducts()}catch(e){console.error(e);toast.error('Could not add demo products. Check Firebase admin rules.')}finally{setSeeding(false)}}
 async function importMajorSpares(){if(bulkImporting)return;setBulkImporting(true);try{const existing=await getDocs(collection(adminDb,'products'));const existingNames=new Set(existing.docs.map(d=>String(d.data().name||'').trim().toLowerCase()));const missing=majorSpareProducts.filter(p=>!existingNames.has(p.name.toLowerCase()));if(!missing.length){toast.success('All 100 major spare products are already in the catalog');return}let added=0;for(let i=0;i<missing.length;i+=10){const batch=missing.slice(i,i+10);await Promise.all(batch.map(p=>addDoc(collection(adminDb,'products'),{...p,rating:Number(Number(p.rating).toFixed(1)),price:Number(p.price),stock:Number(p.stock),catalogSource:'MotoDC major-spares seed'})));added+=batch.length;await loadProducts()}toast.success(`${added} major spare products added`)}catch(e){console.error(e);toast.error('Bulk import stopped. Check Firebase admin rules and try again.')}finally{setBulkImporting(false)}}
 async function remove(id){if(!confirm('Delete this product permanently?'))return;try{await deleteDoc(doc(adminDb,'products',id));setProducts(p=>p.filter(x=>x.id!==id));toast.success('Product deleted')}catch(e){console.error(e);toast.error('Delete failed. Check Firebase admin rules')}}
 if(checking)return <section className="admin page"><div className="empty">Checking admin access...</div></section>;
 if(!user)return <section className="admin page"><div className="adminlogin"><p className="eyebrow">MOTODC ADMIN</p><h1>Admin <span>login.</span></h1><p>Sign in with your Firebase Authentication account.</p><form onSubmit={login}><input type="email" placeholder="Admin email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="heroBtn" disabled={loginBusy}>{loginBusy?'Signing in...':'Sign in'}</button></form></div></section>;
 if(!allowed)return <section className="admin page"><div className="empty"><div><h2>Admin access not granted</h2><p>Your signed-in Firebase admin user does not have an <b>admins/{user.uid}</b> document.</p><button className="heroBtn" onClick={()=>signOut(adminAuth)}>Sign out</button></div></div></section>;
 return <section className="admin page"><div className="adminhead"><div><p className="eyebrow">MOTODC ADMIN</p><h1>Product <span>manager.</span></h1></div><button className="adminsignout" onClick={()=>signOut(adminAuth)}><LogOut size={16}/> Sign out</button></div><div className="adminlayout"><form className="adminform" onSubmit={save}><div className="formtitle"><h2>{editing?'Edit product':'Add product'}</h2>{editing&&<button type="button" onClick={reset}><X size={17}/></button>}</div><label>Name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Product name"/></label><label>Brand<input value={form.brand} onChange={e=>setForm({...form,brand:e.target.value})} placeholder="Brand"/></label><label>Vehicle / fitment<input value={form.fitment} onChange={e=>setForm({...form,fitment:e.target.value})} placeholder="Compatible model or vehicle"/></label><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option>Automobile</option><option>Motorcycle</option></select></label><div className="formrow"><label>Price (₹)<input type="number" min="0" value={form.price} onChange={e=>setForm({...form,price:e.target.value})}/></label><label>Rating<input type="number" min="0" max="5" step="0.1" value={form.rating} onChange={e=>setForm({...form,rating:e.target.value})}/></label></div><label>Stock<input type="number" min="0" value={form.stock} onChange={e=>setForm({...form,stock:e.target.value})}/></label><label>Image URL / path<input value={form.image} onChange={e=>setForm({...form,image:e.target.value})} placeholder="https://... or /products/..."/></label><label>Description<textarea rows="4" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Product description"/></label><button className="heroBtn" disabled={saving}>{saving?'Saving...':editing?'Update product':<><Plus size={17}/> Add product</>}</button></form><div className="adminlist"><div className="listhead"><h2><Package size={20}/> Products</h2><div className="listactions"><b>{products.length}</b><button type="button" className="seedBtn" onClick={importMajorSpares} disabled={bulkImporting}><Download size={15}/>{bulkImporting?'Importing 100...':'Add 100 major spares'}</button><button type="button" className="seedBtn" onClick={seedDemoProducts} disabled={seeding}><Database size={15}/>{seeding?'Adding...':'Add 7 demo products'}</button></div></div>{products.map(p=><div className="adminproduct" key={p.id}><img src={p.image} alt=""/><div><h3>{p.name}</h3><small>{p.brand?`${p.brand} · `:''}{p.category} · ₹{num(p.price).toLocaleString('en-IN')} · {p.rating?`${p.rating}★ · `:''}{p.stock} stock</small>{p.fitment&&<small className="fitment">Fits: {p.fitment}</small>}</div><button onClick={()=>edit(p)} title="Edit"><Edit3 size={16}/></button><button className="delete" onClick={()=>remove(p.id)} title="Delete"><Trash2 size={16}/></button></div>)}{!products.length&&<div className="empty">No products yet.</div>}</div></div><AdminOrders/></section>;
}
