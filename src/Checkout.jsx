import React,{useState,useEffect} from 'react';
import {useNavigate} from 'react-router-dom';
import {auth} from './firebase';
import {ArrowLeft,CheckCircle,MapPin,Wallet,Truck,LogIn,ShieldCheck,LoaderCircle} from 'lucide-react';
import {toast} from 'react-hot-toast';
const money=n=>'₹'+Number(n||0).toLocaleString('en-IN');
const getCart=()=>{try{return JSON.parse(localStorage.getItem('motodc-cart')||'[]')}catch{return[]}};
const clearCart=()=>{localStorage.removeItem('motodc-cart');window.dispatchEvent(new Event('cartchange'))};
const loadRazorpay=()=>new Promise((resolve,reject)=>{if(window.Razorpay)return resolve(true);const existing=document.querySelector('script[src="https://checkout.razorpay.com/v1/checkout.js"]');if(existing){existing.addEventListener('load',()=>resolve(true),{once:true});existing.addEventListener('error',()=>reject(new Error('Payment gateway could not load')),{once:true});return}const s=document.createElement('script');s.src='https://checkout.razorpay.com/v1/checkout.js';s.onload=()=>resolve(true);s.onerror=()=>reject(new Error('Payment gateway could not load'));document.body.appendChild(s)});
const readApiResponse=async response=>{const text=await response.text();let data;try{data=text?JSON.parse(text):{}}catch{throw new Error(response.ok?'Unexpected payment server response':'Payment server error. Please check the Vercel deployment logs.')}if(!response.ok)throw new Error(data.error||'Payment request failed');return data;};
const PROCESSING_MS=4500;
const waitForProcessingWindow=started=>new Promise(resolve=>{const remaining=Math.max(0,PROCESSING_MS-(Date.now()-started));window.setTimeout(resolve,remaining)});
export default function Checkout(){
 const nav=useNavigate(),[items]=useState(getCart()),[placed,setPlaced]=useState(null),[loading,setLoading]=useState(false),[processing,setProcessing]=useState(false),user=auth.currentUser;
 const [form,setForm]=useState({name:user?.displayName||'',email:user?.email||'',phone:user?.phoneNumber?.replace(/^\+91/,'')||'',address:'',city:'',state:'',pincode:''});
 const [payment,setPayment]=useState('cod');
 useEffect(()=>{if(user){loadRazorpay().catch(()=>{});user.getIdToken().catch(()=>{})}},[user]);
 useEffect(()=>{if(!processing)return;const previous=document.body.style.overflow;document.body.style.overflow='hidden';return()=>{document.body.style.overflow=previous}},[processing]);
 const subtotal=items.reduce((a,x)=>a+Number(x.price||0)*Number(x.qty||1),0);const update=e=>setForm({...form,[e.target.name]:e.target.value});
 if(!user)return <section className="page empty checkoutLogin"><div><LogIn size={42}/><p className="eyebrow">ACCOUNT REQUIRED</p><h2>Sign in to complete your order</h2><p>Please sign in before placing an order so your order can be securely linked to your account.</p><button className="heroBtn" onClick={()=>nav('/account')}>Sign In</button></div></section>;
 const validate=()=>{if(!items.length){toast.error('Your cart is empty');nav('/products');return false}if(!/^[0-9]{10}$/.test(form.phone.replace(/\D/g,''))){toast.error('Enter a valid 10-digit phone number');return false}if(!/^[0-9]{6}$/.test(form.pincode)){toast.error('Enter a valid 6-digit PIN code');return false}return true};
 const showConfirmation=async(result,started)=>{await waitForProcessingWindow(started);clearCart();setPlaced({orderId:result.orderId,total:result.total});setProcessing(false);toast.success('Order placed successfully')};
 const placeCod=async()=>{const started=Date.now();setProcessing(true);const token=await user.getIdToken();const response=await fetch('/api/place-cod',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({items:items.map(x=>({id:x.id,qty:x.qty})),customer:{name:form.name,email:form.email,phone:form.phone},shipping:{address:form.address,city:form.city,state:form.state,pincode:form.pincode}})});const result=await readApiResponse(response);await showConfirmation(result,started)};
 const placeOnline=async()=>{const [_,token]=await Promise.all([loadRazorpay(),user.getIdToken()]);const create=await fetch('/api/create-payment',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({items:items.map(x=>({id:x.id,qty:x.qty})),amount:subtotal})});const created=await readApiResponse(create);const options={key:created.keyId,amount:created.amount,currency:created.currency,name:'MotoDC',description:'Automotive parts order',order_id:created.orderId,prefill:{name:form.name,email:form.email,contact:form.phone},theme:{color:'#ff3157'},handler:async response=>{const started=Date.now();setProcessing(true);try{const verify=await fetch('/api/verify-payment',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({razorpay_order_id:response.razorpay_order_id,razorpay_payment_id:response.razorpay_payment_id,razorpay_signature:response.razorpay_signature,customer:{name:form.name,email:form.email,phone:form.phone},shipping:{address:form.address,city:form.city,state:form.state,pincode:form.pincode},paymentMethod:'online'})});const result=await readApiResponse(verify);await showConfirmation(result,started)}catch(e){setProcessing(false);toast.error(e.message||'Payment verification failed')}}};const rzp=new window.Razorpay(options);rzp.on('payment.failed',response=>toast.error(response.error?.description||'Payment failed'));rzp.on('dismiss',()=>{});rzp.open()};
 const submit=async e=>{e.preventDefault();if(!validate())return;setLoading(true);try{if(payment==='cod')await placeCod();else await placeOnline()}catch(err){console.error(err);setProcessing(false);toast.error(err.message||'Could not place the order')}finally{setLoading(false)}};
 if(processing)return <section className="page checkoutProcessing" aria-live="polite"><div className="processingCard"><div className="processingIcon"><LoaderCircle size={42}/></div><p className="eyebrow">ORDER PROCESSING</p><h1>Please <span>wait...</span></h1><p>{payment==='online'?'Your payment was received. We are confirming your order securely.':'We are placing your order securely.'}</p><div className="processingBar"><span/></div><small>Please do not refresh or close this page.</small></div></section>;
 if(placed)return <section className="page checkoutPage"><div className="orderSuccess"><CheckCircle size={64}/><p className="eyebrow">ORDER CONFIRMED</p><h1>Thank you for your <span>order.</span></h1><p>Your order has been placed successfully.</p><div className="orderNumber">Order ID <strong>{placed.orderId}</strong></div><h2>{money(placed.total)}</h2><div className="successActions"><button className="heroBtn" onClick={()=>nav('/products')}>Continue Shopping</button><button className="secondaryBtn" onClick={()=>nav('/')}>Back Home</button></div></div></section>;
 if(!items.length)return <section className="page empty"><h2>Your cart is empty</h2><button className="heroBtn" onClick={()=>nav('/products')}>Browse Products</button></section>;
 return <section className="page checkoutPage"><button className="backBtn" onClick={()=>nav('/cart')}><ArrowLeft size={16}/> Back to Cart</button><div className="pagehead"><p className="eyebrow">SECURE CHECKOUT</p><h1>Complete your <span>order.</span></h1><p>Enter your delivery details and choose a payment method.</p></div><div className="checkoutLayout"><form className="checkoutForm" onSubmit={submit}><div className="checkoutBlock"><h2>Contact details</h2><div className="formGrid"><label>Full name<input required name="name" value={form.name} onChange={update} placeholder="Your full name"/></label><label>Email<input required type="email" name="email" value={form.email} onChange={update} placeholder="you@example.com"/></label><label>Phone<input required name="phone" value={form.phone} onChange={update} placeholder="10-digit mobile number" maxLength={10}/></label></div></div><div className="checkoutBlock"><h2><MapPin size={19}/> Delivery address</h2><div className="formGrid"><label className="full">Address<input required name="address" value={form.address} onChange={update} placeholder="House / street / area"/></label><label>City<input required name="city" value={form.city} onChange={update} placeholder="City"/></label><label>State<input required name="state" value={form.state} onChange={update} placeholder="State"/></label><label>PIN code<input required name="pincode" value={form.pincode} onChange={update} placeholder="6-digit PIN" maxLength={6}/></label></div></div><div className="checkoutBlock"><h2><Wallet size={19}/> Payment method</h2><div className="paymentOptions"><label className={payment==='cod'?'selected':''}><input type="radio" name="payment" checked={payment==='cod'} onChange={()=>setPayment('cod')}/><span><b>Cash on Delivery</b><small>Pay when your order arrives</small></span></label><label className={payment==='online'?'selected':''}><input type="radio" name="payment" checked={payment==='online'} onChange={()=>setPayment('online')}/><span><b>Online Payment</b><small>Secure Razorpay checkout</small></span></label></div></div><button className="heroBtn placeOrder" disabled={loading}>{loading?'Processing...':payment==='online'?<>Pay Securely · {money(subtotal)} <ShieldCheck size={17}/></>:<>Place Order · {money(subtotal)}</>}</button></form><aside className="checkoutSummary"><h2>Order Summary</h2>{items.map(x=><div className="summaryItem" key={x.id}><img src={x.image} alt=""/><div><b>{x.name}</b><small>Qty {x.qty}</small></div><strong>{money(x.price*x.qty)}</strong></div>)}<hr/><p>Subtotal <b>{money(subtotal)}</b></p><p>Delivery <b>FREE</b></p><h3>Total <span>{money(subtotal)}</span></h3><div className="trust"><Truck size={17}/> Free delivery on this order</div></aside></div></section>;
}
